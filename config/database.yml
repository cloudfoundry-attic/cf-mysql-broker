
development:
  adapter: mysql2
  encoding: utf8
  database: development
  pool: 5
  username: root

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  encoding: utf8
  database: test
  pool: 5
  username: root
  # These are required by the tests, even though they have default values
  password:
  host: localhost
  port: 3306
